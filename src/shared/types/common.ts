/**
 * 공통 타입 정의
 */

// 언어 관련 타입
export type Language = "en" | "ko";

// 기기 타입 (기존 environment.ts와 통합)
export type Device = "android" | "ios" | "web";

// 환경 타입
export type Environment = "dev" | "stage" | "prod";
